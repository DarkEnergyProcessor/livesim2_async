local AquaShine = ...
local Node = AquaShine.Node
local love = love

local MainMenuButton = Node.Image:extend("Livesim2.MainMenuButton")

function MainMenuButton.init(this, text)
	
end

return MainMenuButton
